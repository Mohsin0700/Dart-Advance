void main() {
  // Set<String> names = {"Mohsin", "Arbab", "Ali", "Munawar", "Adnan", "Shahid"};
  // print(names);
  // print(names.indexed);
  // print(names.elementAt(4));
  // A set is similar to the list in dart but it ignores any duplicate value by default and only carries unique values.
  // One usecase which came to mind while working with it is let's try to removes duplicates from a list
  // List<int> nums = [2, 1, 2, 3, 3, 3, 2, 4, 5, 6];

  // print("List 1 before modification: $nums");

  // Set<int> nums2 = {};

  // nums.forEach((item) {
  //   nums2.add(item);
  // });

  // nums.clear();

  // nums2.forEach((item) {
  // nums.add(item);
  // });

  // print("List 1 after modification: $nums");

  // List<int> nums = [3, 1, 2, 5, 4];
  // print('Given List: $nums');

  // Let's sort this list in ascending order
  // List<int> newNums = [];

  // List<int> remaining = nums;
  // while (remaining.isNotEmpty) {
  //   int smallest = remaining[0];

  //   for (var i = 0; i < remaining.length; i++) {
  //     if (smallest > remaining[i]) {
  //       smallest = remaining[i];
  //     }
  //   }
  //   newNums.add(smallest);
  //   remaining.remove(smallest);
  // }

  // print('Modified List: $newNums');

  // Let's sort this list in descending order
  // while (remaining.isNotEmpty) {
  //   int largest = remaining[0];
  //   for (var i = 0; i < remaining.length; i++) {
  //     if (largest < remaining[i]) {
  //       largest = remaining[i];
  //     }
  //   }
  //   newNums.add(largest);
  //   remaining.remove(largest);
  // }

  // print('Modified List in descending order is ${newNums}');
}
